// Inter-sentence flow detection (lexical bridges, transitions).
// TODO: port FLOW_STOPWORDS, FLOW_TRANSITIONS, computeFlowFlags from extension content.js.

export function computeFlowFlags(_sentenceTexts) {
  return [];
}
