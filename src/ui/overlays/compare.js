// AI-rewrite comparison overlay (original vs AI side-by-side).
// TODO: port rewriteFullText, openCompare, closeCompare from extension content.js.

export function openCompare(_originalText, _rewriteText, _opts) {}
export function closeCompare() {}
export async function rewriteFullText() {}
